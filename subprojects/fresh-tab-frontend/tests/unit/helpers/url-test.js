import { expect } from 'chai';

import { describe, it } from 'mocha';
import { url } from 'fresh-tab/helpers/url';

describe('Unit | Helper | url', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = url(42);
    expect(result).to.be.ok;
  });
});

