import { expect } from 'chai';

import { describe, it } from 'mocha';
import { highlight } from 'fresh-tab/helpers/highlight';

describe('Unit | Helper | highlight', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = highlight('42');
    expect(result).to.be.ok;
  });
});

