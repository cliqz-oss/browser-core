<a class="result" data-url="{{url}}" href="{{displayUrl}}">
    {{> partials/logo}}
    {{> partials/abstract}}
</a>

<div class="padded">
  <div class="lotto white-background">
    <p class="lotto-date">{{ local 'lotto_gewinnzahlen' }} &#8226; {{ localeDate }}</p>
    {{#each lottoResults}}
      <div class="row">
        <div class="lotto-item-wrapper {{ classNames }}">
          {{#each result}}
            <div class="item">
              {{ this }}
            </div>
          {{/each}}
        </div>
        {{#if description}}
          <span class="description">{{ local description }}</span>
        {{/if}}
        {{#if table}}
          <div id="lotto-table">
            <div class="table-head">
              {{#each table.columns}}
                <span>{{ local this }}</span>
              {{/each}}
            </div>
            {{#each table.data}}
              <div class="table-row">
                {{#each this}}
                    <span>
                        {{#each this}}
                            {{ this }}<br />
                        {{else}}
                            {{ this }}
                        {{/each}}
                    </span>
                {{/each}}
              </div>
            {{/each}}
          </div>
        {{/if}}
      </div>
    {{/each}}
    <p class="no-guarantee">{{ local 'no_guarantee' }}</p>
  </div>
</div>

{{> partials/buttons}}
